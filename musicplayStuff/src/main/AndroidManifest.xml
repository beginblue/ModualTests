<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="blue.person.musicplaystuff">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <service
            android:name="blue.person.musicplaystuff.musicplayserv.MusicPlayServ"
            android:enabled="true"
            android:exported="true" />
        <!--<receiver android:name=".musicControl.PlayController$onCompletionBroadcastReceiver">-->
            <!--<intent-filter>-->
                <!--<action android:name="blue.broadcast.NEXT_MUSIC" />-->
            <!--</intent-filter>-->
        <!--</receiver>-->
    </application>

</manifest>